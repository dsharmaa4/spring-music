apiVersion: v1
kind: Service
metadata:
  name: spring-music-service
  namespace: SOME_INVALID_NAMESPACE_REPLACE_ME
spec:
  type: LoadBalancer
  selector:
    app: spring-music
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
