apiVersion: v1
kind: Service
metadata:
  name: spring-music-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: spring-music
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
